<UserControl x:Class="BudgetAnalyser.MainMenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:b="clr-namespace:BudgetAnalyser"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance b:MainMenuController, IsDesignTimeCreatable=True}">

    <UserControl.Resources>

        <!-- Material top-app-bar tab: flat, ripple feedback on press, underline indicator when active -->
        <Style x:Key="TabButtonStyle"
               TargetType="{x:Type ToggleButton}">
            <Setter Property="Margin"
                    Value="0,0,4,0" />
            <Setter Property="Padding"
                    Value="16,0" />
            <Setter Property="Height"
                    Value="48" />
            <Setter Property="MinWidth"
                    Value="120" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontWeight"
                    Value="Medium" />
            <Setter Property="Foreground"
                    Value="{StaticResource Brush.Text.Disabled}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="3" />
                            </Grid.RowDefinitions>
                            <materialDesign:Ripple Grid.Row="0"
                                                    Focusable="False"
                                                    Feedback="{DynamicResource PrimaryHueMidBrush}"
                                                    HorizontalContentAlignment="Center"
                                                    VerticalContentAlignment="Center">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </materialDesign:Ripple>
                            <Border x:Name="ActiveIndicator"
                                    Grid.Row="1"
                                    Height="3"
                                    Background="{StaticResource Brush.ModernTile.Highlight}"
                                    CornerRadius="1.5"
                                    Opacity="0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="ActiveIndicator"
                                        Property="Opacity"
                                        Value="1" />
                                <Setter Property="Foreground"
                                        Value="{StaticResource Brush.Text.Default}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Foreground"
                                        Value="{StaticResource Brush.Text.Default}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource Brush.MainBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="6"
                               Color="Black"
                               Direction="270"
                               Opacity="0.5"
                               ShadowDepth="2" />
        </Grid.Effect>

        <ToggleButton Grid.Column="0"
                      Margin="12,0,4,0"
                      IsChecked="{Binding DashboardToggle}"
                      Command="{Binding ShowDashboardCommand}"
                      Style="{StaticResource TabButtonStyle}">
            <TextBlock Text="Dashboard" />
        </ToggleButton>

        <ToggleButton Grid.Column="1"
                      IsChecked="{Binding TransactionsToggle}"
                      Command="{Binding ShowTransactionsCommand}"
                      Style="{StaticResource TabButtonStyle}">
            <TextBlock Text="Transactions" />
        </ToggleButton>

        <ToggleButton Grid.Column="2"
                      IsChecked="{Binding LedgerBookToggle}"
                      Command="{Binding ShowLedgerBookCommand}"
                      Style="{StaticResource TabButtonStyle}">
            <TextBlock Text="Ledger Book" />
        </ToggleButton>

        <ToggleButton Grid.Column="3"
                      IsChecked="{Binding BudgetToggle}"
                      Command="{Binding ShowBudgetCommand}"
                      Style="{StaticResource TabButtonStyle}">
            <TextBlock Text="Budget" />
        </ToggleButton>

        <ToggleButton Grid.Column="4"
                      IsChecked="{Binding ReportsToggle}"
                      Command="{Binding ShowReportsCommand}"
                      Style="{StaticResource TabButtonStyle}">
            <TextBlock Text="Reports" />
        </ToggleButton>
    </Grid>
</UserControl>
